export const serverPort: number = 8080;
export const serverHost: string = 'localhost';
export const serverProtocol: string = 'http';
export const prefix: string = 'product';
export const serverUrl: string = `${serverProtocol}://${serverHost}:${serverPort}/${prefix}`;
